# Pi Portal - Docker Environment Variables
# Copy this file to .env.docker and customize as needed

# Server Configuration
PI_PORTAL_SERVER_PORT=8000

# Optional: Override other settings
# PI_PORTAL_PI_EXECUTABLE=/usr/bin/pi
# PI_PORTAL_PI_SESSION_DIR=/data/pi_sessions
# PI_PORTAL_SERVER_RELOAD=false

# Note: Most settings are configured in docker-compose.yml
# Only override here if you need different values
